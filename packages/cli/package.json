{
  "name": "@gitgov/cli",
  "version": "1.8.0",
  "description": "🚀 AI-first governance CLI for intelligent work. Install with `npm install -g @gitgov/cli` and start governing your projects with interactive dashboards, task management, and workflow automation.",
  "type": "module",
  "main": "build/dist/gitgov.mjs",
  "bin": {
    "gitgov": "./build/dist/gitgov.mjs"
  },
  "files": [
    "build/dist"
  ],
  "scripts": {
    "clean": "rm -rf build",
    "build": "pnpm clean && mkdir -p build/dist && esbuild src/index.ts --bundle --platform=node --format=esm --target=node20 --outfile=build/dist/gitgov.mjs --jsx=automatic --jsx-import-source=react --external:react-devtools-core --external:commander --external:ink --external:react --external:assert --external:events --external:util --external:path --external:fs --external:crypto --external:child_process --external:clipboardy --external:@gitgov/core && chmod +x build/dist/gitgov.mjs",
    "build:npm": "./scripts/build-npm.sh",
    "start": "./build/dist/gitgov.mjs",
    "dev": "tsx src/index.ts",
    "test": "jest",
    "test:all": "pnpm build && pnpm test && pnpm pack:build",
    "prepublishOnly": "pnpm build",
    "pack:build": "mkdir -p build && pnpm pack && mv *.tgz build/",
    "release:npm": "./scripts/release-npm.sh",
    "release:github": "./scripts/release-github.sh",
    "version:bump": "./scripts/version.sh",
    "verify": "pnpm build && pnpm test && echo 'All checks passed!'"
  },
  "keywords": [
    "gitgovernance",
    "ai-first",
    "workflow",
    "governance",
    "cli",
    "tui",
    "npm",
    "project-management",
    "task-management",
    "dashboard",
    "interactive",
    "productivity",
    "automation",
    "collaboration"
  ],
  "author": "GitGovernance Team",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gitgovernance/monorepo.git",
    "directory": "packages/cli"
  },
  "homepage": "https://github.com/gitgovernance/monorepo/tree/main/packages/cli#readme",
  "bugs": {
    "url": "https://github.com/gitgovernance/monorepo/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@gitgov/core": "^1.3.0",
    "clipboardy": "^5.0.0",
    "commander": "^11.1.0"
  },
  "optionalDependencies": {
    "ink": "^4.4.1",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^9.2.6",
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.24",
    "conventional-changelog-conventionalcommits": "^7.0.2",
    "dotenv": "^17.2.2",
    "esbuild": "^0.19.0",
    "ink-testing-library": "^4.0.0",
    "jest": "^29.0.0",
    "semantic-release": "^22.0.12",
    "ts-jest": "^29.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
