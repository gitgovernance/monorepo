{
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "tsx": "^4.16.2",
    "markdown-link-check": "^3.12.1"
  },
  "scripts": {
    "cli": "pnpm --filter @gitgov/cli dev",
    "build": "pnpm -r build",
    "build-blueprints": "bash .gitgov/scripts/compiler/build.sh",
    "build-all-blueprints": "node .gitgov/scripts/compiler/build-all.js",
    "check": "pnpm test && pnpm build",
    "test": "pnpm -r test"
  }
}